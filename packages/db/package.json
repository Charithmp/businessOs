{
  "name": "@bos/db",
  "private": true,
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "generate": "cd ../.. && prisma generate --schema packages/db/prisma/schema.prisma",
    "migrate": "cd ../.. && prisma migrate dev --schema packages/db/prisma/schema.prisma",
    "build": "prisma generate",
    "lint": "echo database schema checked",
    "test": "echo database tests live with the API"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  }
}
